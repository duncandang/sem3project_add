# ==============================================================================
# OpenLane 2 Project Template .gitignore
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. OpenLane 2 Run Databases & Outputs (Regenerated each run)
# ------------------------------------------------------------------------------
# The runs/ directory contains massive intermediate layout databases, def, odb, 
# gds, and extraction reports (e.g., runs/RUN_2026-08-25_10-03-57/). These files 
# are extremely large and should be kept local.
runs/

# ------------------------------------------------------------------------------
# 2. Python Virtual Environments (Non-portable, machine-specific)
# ------------------------------------------------------------------------------
# Ignore python virtual environments used to run OpenLane 2 locally.
# Recreate them using 'pip install -r requirements.txt' instead.
venv/
.venv/
openlane2-venv/
bin/
lib/
lib64/
include/
pyvenv.cfg

# ------------------------------------------------------------------------------
# 3. Process Design Kit (PDK) Data
# ------------------------------------------------------------------------------
# Volare PDK manager data (usually stored in $HOME/.volare, but if managed
# locally inside the project, exclude it).
.volare/
pdk/

# ------------------------------------------------------------------------------
# 4. OS & Editor Temporary/Backup Files
# ------------------------------------------------------------------------------
# Editor backup files (nano, vim, emacs)
*~
*.swp
*.swo
.nfs*

# macOS specific files
.DS_Store

# Windows specific files
Thumbs.db
desktop.ini

# ------------------------------------------------------------------------------
# 5. Python Compilation Caches
# ------------------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# ------------------------------------------------------------------------------
# 6. User-specific Configuration Backups or Command Logs
# ------------------------------------------------------------------------------
# Logging or temporary tool logs generated during manual execution
*.log
!flow.log     # Keep flow.log if you explicitly want to track top-level runs in git
